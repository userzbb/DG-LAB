//! TUI 应用（待实现）

/// TUI 应用状态
#[allow(dead_code)]
pub struct TuiApp;

impl TuiApp {
    /// 创建新的 TUI 应用
    #[allow(dead_code)]
    pub fn new() -> Self {
        Self
    }
}

impl Default for TuiApp {
    fn default() -> Self {
        Self::new()
    }
}
